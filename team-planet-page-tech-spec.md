# 共享團隊頁 — 技術方案（構思階段草案）

> 狀態：構思中，未開始開發。呢份文件係俾你決定範圍用嘅技術方案，唔係實施中嘅任務清單。

## 1. 概念總結

用戶（partner 網站訪客）進入頁面 → 全螢幕自動播放 intro 短片（13 位成員頭像每 3 秒循環，黑白、對焦眼睛）→ 最後一位眼睛化光 → 轉場成星空，每粒星球代表一位成員 → 用戶指向/撳星球睇資訊。

## 2. 頁面架構建議

**獨立頁面，唔併入 `index.html`**，跟 `/blog/base`、`/blog/luma` 嘅模式：新開 `/team/index.html`（或 `team.tbchk.com` 子網域，視乎你揀嘅連結方式）。

理由：
- 呢個功能同主站無直接業務邏輯關連（唔涉及 industry 個人化、lead capture 邏輯）
- 獨立頁面唔會拖慢或搞亂 `index.html`（已經 10,600+ 行）
- Partner 連結方式（iframe / 直接連結 / 子網域）你話未決定 — 獨立頁面對三種方式都通用，遲啲先揀連結方式都唔使重寫頁面本身

**連結方式決定咗先做嘅嘢**（你答咗「未決定」，記錄喺度，等你搏埋再做）：
- 獨立連結 → 淨係要一個靚 URL + 各 partner 網站加返個 CTA/link
- iframe 嵌入 → 要額外處理 `X-Frame-Options`、response viewport 適配、同埋 iframe 入面點樣跳走去 TBC 主站（例如頁尾一粒「了解 TBC」按鈕）

## 3. 資料結構（成員名單）

建議用一個簡單 JS array（同主站 `INDUSTRY_*` 嘅寫法一致，唔使起 CMS/後台）：

```js
const TEAM_MEMBERS = [
  {
    id: "grouper",
    name: "Grouper 石斑",
    company: "TBC Solutions",
    role: "創辦人",
    bio: "一句簡介，20-30字",
    avatar: "/team/avatars/grouper-bw.jpg",
    planetTexture: "rocky-purple",
    contact: { whatsapp: "85263920913", linkedin: "" }
  },
  // ... 共 13 個，partner 成員可以陸續加
];
```

管理方式：初期直接改呢個 array（同你依家管理 industry 內容嘅方式一樣，你已經好熟）。日後成員多咗、換人頻密，先再考慮做返個簡單後台。

## 4. Intro 短片實現方式（兩個選項）

| 方式 | 優點 | 缺點 |
|---|---|---|
| **A. 預先錄製嘅影片檔（mp4）** | 畫面效果最穩定、可以叫設計/剪片做到好靚嘅光影轉場 | 換人/換 partner 就要重新剪片同上載 |
| **B. CSS/JS 即時合成**（推薦） | 換成員淨係改 array，唔使重新剪片；頭像同星球資料互通 | 轉場特效要用 CSS animation 砌，靚度受限於前端功力 |

**建議：方式 B**，因為你哋 partner 名單會變動，方式 A 每次換人都要重新製作片段，維護成本太高。轉場特效（化光效果）可以用 CSS `filter: brightness()` + `box-shadow` 光暈 + `scale` 動畫做到接近效果，唔使真係渲染片。

## 5. 頭像處理

你話初型可以用 AI 生成、正式版用真人相。建議流程：

1. 統一構圖：頭至膊頭、正面/微側、對焦眼睛（等所有人張相風格一致，先夠「掃描感」）
2. 統一後製：黑白 + 對比度調高 + 暗角（vignette）—— 可以寫一個簡單腳本（Sharp/PIL）批量處理，唔使逐張手調
3. 檔案要求：正方形、建議 ≥800×800px（度片會 crop 做圓形頭像）

真人相未夠齊之前，可以用 AI 生成嘅「西裝男/女半身照」頂住個位，上線前換返真人相 —— 呢個係方式 B 嘅好處，換相唔使改邏輯。

## 6. 星空 / 星球導覽

**星球視覺參考**：建議用 NASA 公開圖庫嘅行星紋理（NASA Visible Earth / Solar System 系列圖，公有領域、可自由使用）做 texture，再用 CSS `radial-gradient` 疊加光影做立體感 —— 唔使做真 3D 都有質感。如果想要真係識轉嘅 3D 星球，先要引入 Three.js（同主站依家零 heavy 依賴嘅原則有出入，建議留返做後期加強）。

**排位方式**：13 粒（甚至日後更多）星球建議「人手定位」（同我個原型一樣，逐粒寫死 % 座標），因為咁樣可以確保靚、唔會疊埋一齊。人數再多先考慮做自動排版演算法。

**互動**：
- 桌面：hover 出資訊卡
- 手機：撳出底部彈出卡（bottom sheet），唔用 hover（手機冇 hover）
- 資訊卡內容：姓名、職稱、所屬 partner、一句簡介、聯絡 CTA（WhatsApp/LinkedIn）

**觸控熱區**：我個原型嘅星球淨係 22px，手機上太細（WCAG 建議觸控熱區 ≥44px）。正式版星球視覺可以細，但點擊熱區要用透明大 hit-area 包住。

## 7. 無障礙 / 體驗要注意

- Intro 片段長達 39 秒（13 人 × 3 秒）全自動播放且「播完先有功能用」—— 呢個做法有 WCAG 疑慮（自動播放超過 5 秒嘅內容，用戶應該可以暫停/跳過）。建議：3 秒後喺角落淡入一個細細嘅「跳過」字樣（唔影響視覺主體，但俾番選擇權用戶，尤其係回頭客唔想每次都等 39 秒）
- 支援 `prefers-reduced-motion`：對呢個設定嘅用戶，直接跳去星空畫面，唔播 intro
- 手機流量：13 張相 + 星球 texture 建議做 lazy preload（喺 intro 播緊嗰陣背景預載星空資源），避免轉場卡頓

## 8. 技術棧建議

跟主站一致嘅原則——**輕量、零建置步驟、vanilla HTML/CSS/JS**：
- 頭像循環、化光轉場：CSS animation + JS 控制時序
- 星球排位同互動：純 `<div>` + CSS（唔使 canvas/WebGL）
- 如果日後想加「星球慢慢自轉」「滑鼠視差」等進階效果，先分階段引入，唔一開始就上重型 3D 庫

## 9. 建議分期

| 階段 | 內容 |
|---|---|
| Phase 1（已完成） | 概念原型（我畀你睇嗰個互動 demo） |
| Phase 2 | 落實真實/過渡用嘅 13 人名單 + 頭像 + 簡介文案 |
| Phase 3 | 決定連結方式（獨立連結 / iframe / 子網域）— 你話未決定，呢步等你哋內部同 partner 傾掂 |
| Phase 4 | 正式開發獨立頁面 `/team/`，接入真實資料 |
| Phase 5 | Partner 網站逐個加返連結/CTA，上線 |

## 10. 仲要你決定嘅嘢（先開始 Phase 4）

1. **連結方式**：獨立連結 / iframe / 子網域 —— 三選一（或者未諗掂都可以，唔急住呢步）
2. **13 人名單**：邊 13 個人？淨係 TBC 定包埋合作 partner 嘅人？定期點更新？
3. **Intro 可唔可以跳過**：出於無障礙同回頭客體驗，建議加個細細嘅「跳過」選項 —— 你同唔同意呢個做法，定係堅持一定要行足 39 秒？
